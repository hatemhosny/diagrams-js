---
title: Installation
description: Installing diagrams-js in your project
---

# Installation

## Node.js

Install [`diagrams-js`](https://www.npmjs.com/package/diagrams-js) using your preferred package manager:

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import CodeBlock from "@theme/CodeBlock";
import Admonition from "@theme/Admonition";

<Tabs groupId="install">
  <TabItem value="npm" label="npm" default>
    <CodeBlock language="bash">{`npm install diagrams-js`}</CodeBlock>
  </TabItem>
  <TabItem value="yarn" label="yarn" default>
    <CodeBlock language="bash">{`yarn add diagrams-js`}</CodeBlock>
  </TabItem>
  <TabItem value="pnpm" label="pnpm" default>
    <CodeBlock language="bash">{`pnpm add diagrams-js`}</CodeBlock>
  </TabItem>
  <TabItem value="bun" label="bun" default>
    <CodeBlock language="bash">{`bun add diagrams-js`}</CodeBlock>
    <Admonition type="info" title="Bun Version">
      Bun 1.3.0+ is required for diagrams-js to work properly due to WebAssembly
      support improvements.
    </Admonition>
  </TabItem>
</Tabs>

### Optional Dependencies

[Rendering](../guides/rendering.mdx) SVG does not require any additional packages. However, for PNG and JPG rendering in Node.js, the package `sharp` is required:

<Tabs groupId="install">
  <TabItem value="npm" label="npm" default>
    <CodeBlock language="bash">{`npm install sharp`}</CodeBlock>
  </TabItem>
  <TabItem value="yarn" label="yarn" default>
    <CodeBlock language="bash">{`yarn add sharp`}</CodeBlock>
  </TabItem>
  <TabItem value="pnpm" label="pnpm" default>
    <CodeBlock language="bash">{`pnpm add sharp`}</CodeBlock>
  </TabItem>
  <TabItem value="bun" label="bun" default>
    <CodeBlock language="bash">{`bun add sharp`}</CodeBlock>
  </TabItem>
</Tabs>

## Browser Usage

### Using a Bundler

Install via npm and import:

```typescript
import { Diagram } from "diagrams-js";
import { EC2 } from "diagrams-js/aws/compute";
```

### Using CDN

```html
<script type="module">
  import { Diagram } from "https://esm.sh/diagrams-js";
  import { EC2 } from "https://esm.sh/diagrams-js/aws/compute";
</script>
```

### Using CDN with Import Map

You can use import maps to enable bare module imports while loading from a CDN:

```html
<script type="importmap">
  {
    "imports": {
      "diagrams-js": "https://esm.sh/diagrams-js",
      "diagrams-js/": "https://esm.sh/diagrams-js/"
    }
  }
</script>

<script type="module">
  import { Diagram } from "diagrams-js";
  import { EC2 } from "diagrams-js/aws/compute";
  import { RDS } from "diagrams-js/aws/database";

  const diagram = Diagram("My Diagram");
  const web = diagram.add(EC2("Web Server"));
  const db = diagram.add(RDS("Database"));

  web.to(db);

  const svg = await diagram.render();
  document.body.innerHTML = svg;
</script>
```

:::tip Import Maps
Import maps allow you to use bare module specifiers (like `diagrams-js/aws/compute`) instead of full URLs, making your code cleaner and more portable between bundler and CDN environments.
:::

## Next Steps

- Follow the [Quick Start guide](./quickstart.mdx) to create your first diagram
- Explore the [Examples](./examples.mdx) for real-world use cases
- Check out the [Node Reference](../guides/providers.mdx) for all available providers and services
